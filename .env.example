# Environment variables for Naruto-mcp-V2

# If using Ollama locally, point to the local endpoint (default shown)
OLLAMA_URL=http://localhost:11434

# If you ever switch to OpenAI, set this (not required for local Ollama)
# OPENAI_API_KEY=
